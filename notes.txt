do not take the diag but average a few points around it.
pooler les essais du loc et de 1obj pour améliorer la generalization.?

BACK TO V1
c1: windows: .15-.3s for early and .4-.55 for late
v2: windows now .1-.4s for early and .45-.75s for late.
v3: cat10, no reduc dim. All launching all decoding. 
v4: now clipping - baselining - clipping again. All conds, including delay windowing 
v5: reduc dim for single ch decoding + smaller time window
v6: including the presence of a flash in the metadata. Also Decoding cimplexitiy with generalization (sameShape -> sameColor). + mean 3
v7: testing split queries for OVR + better way to arange for split queries aleas. TO BE REPLICATED IN THE NORMAL DECODING
v8: quality threshold = 0.75 (decoding matching in a signle block with perf >.75) -> worse than original.
v9: new Epochs named Epochs_filt with lowpass=30hz, highpass=0.1hz (instead of 0 and 0.01), - Launched both at 13h55 
Finally corrected split_queries for OVR ! to be tested
Corrected test-cond as well, there was a bug due to the change of shape of all_models (not for OVR)
Actually the new epochs is the same as the old, more or less. The sfreq is 50hz, so there is a 25hz lowpass. Only highpass at 0.1hz is different
v10: autoreject + filter with Perf==1 for all - good ! but still "hashy", need to smooth more. Maybe increase sfreq too...
v11: Simple ridgeCV and equalize events for all conds (check for OVR if it works!) + trying complexity regression decoding (with equalize too, maybe try without) + single_channel decoding.
add complexity decoding!
v12: 100hz - still with ridgeCV ! - still equalize_events
v13: 5cat and 11smooth (insread of 21) regression only - CHANGING, now 10cat 21smooth
v14: 10cat regression only - still equalize_events
v15: no cat no mean no equalize events -> best !
v16: 21smooth, Ridge, no equalize. SameX OVR, mismatch with complexity splits, normal and OVR
v17: response-lock normal decoding + decoding window with XGboost, no reduc dim, no param search
v18: all LRcv with 5cat - 21smooth + also windowXGBoost same as v17, corrected some queries (but not all it seems) -- small problem on resplock: cropped to epochs before getting new epochs -- pretty good perf !
decoding window with XGboost, .99reduc dim, no param search, micro_ave=2, max_trials_win=5000, sfreq_win=50hz -> bof
v19: 50hz ; trial micro-averaging, 2000 trials. Trial microaveraging makes split queries VERY costly. takes 5x time to run. So removed most split queries for v19.
+ decoding window with XGboost, .99reduc dim, no param search, micro_ave=2, max_trials_win=False, sfreq_win=50hz. new: left color and shape, mismatch side. New windows: from 100 to 700ms after word onset. 600ms during the delay.
v20: Pyriemann? Works only for windows decoding, because the covariance is computed over time (the time dimension is lost). Perf is very bad
Trying again without pyriemann but with equalize events. 20000max trials, 50hz, .99reducdim
also trying side mismatch decoding with micro_ave=3, equalize events, 10000maxtrials, window5-8.5s.
Also trying CCA
launched v21, all freq bands -- watch out low and high gamma already failed due to hfreq > sfreq
v22: filtering 0.01hz-100hz - not much better? maybe a bit better!
v23: proper CCA with cat10, nofitler, 50hz. 
dimensionality, cat10, with trials averaging, but its shit so trying without trials averaging
Maybe try subtract evoked?
v24: master decoder: 100hz, nofilter, cat5, autoreject, NO micro-average. for color, shape and mismatch only.
Not excellent. 
Launched regression and Side with Not the same color on the other side.
v25, 26, 27, looking for best perf. 25 is bad. 26 takes forever.

Dimensionality: NOW using time windows !
v28 = copy of v25 but with linear detrending
v29: same with localizer (single words loc). Also Decoding ovr with auc_thresh=.55
v31: dimensionality analysis with singleobjects localizer -> bof, trying with averaging trials
v32: dim with cross word-img localzier, bof trying with avertaging trials. bof, trying without detrending.
v33: dim with riemann. bof, trying overlapping windows


Gen of localised decoders : do we see the maintenance of representation waiting to be composed? Probably not. + for classifiers trained on one obj blocks, the one should be only to the following property!
-> well first the shape decoder doesnt generalize well from loc to blocks ...
Then, how do you get the "diagonal" generalization performance? To be fair you would need to select a single decoder and test it on each timepoint.

Dimensionality analysis: decrease during the delay? Bigger for less complex sentences? Bingo ! Not really the case ...
remove windows? do not need them since we use the localizer.

Localizer for decoding. -> ok for lots of conditions ! should try with more folds

Test new localiser with both words and images. Should be done by Monday evening. 




cosine distance between decoders, as a timegen?


TODO: test on subset of all analyses! eg: prop of one object, button, complexité.
Temps-fréquence?

Maybe use only half of the trials (even-odd) LAUNCHED for v18 and 19 (19 looks best) -> perf is too low because we use only half of the "positive"class trials. 
-> doing the split only for the "negative" class. Launched for v18 and v19.

RT split (3 quantiles)



XGBoost with all subjects? -> chance level even for easy stuff...
SameX has super generalization because the negative class is the same in train and test generalization
remove perf filtering for windows analyses?
XGBoost on all subjects at once? With regression for each property. But need an overall PCA, not in the pipeline obj.

Resplock for regression? and for window?


smooth AUC curve ?

##, reducdim=0.99, 5folds, parameters search with 200iter


no Cmismatch and Smismatch yet + oneobj Perf, Matching and Button, need to relaunch OVR decoding
Generalization of decoders? Using sliding windows
Check the windows of single channels decoding: should it be 1 SOA ?
use only windows for testing generalization of proprty decoders?  with offset for subject with slower presentation



Milad Mozafari: decoding text from brain representations of images works very well (nearly as good as predicting text from text), but the other way around is very bad ...



jobs: 


change Error_type to lower case? Would be great for grepping logs.

np.diagonal(array, offset) to get off-diagonals

RSA on image presentation to see if the similarity is bigger when the shape matches, or the color




Use principal angles between subspaces? https://docs.scipy.org/doc/scipy/reference/generated/scipy.linalg.subspace_angles.html
angle between first and second color (or shape) encoding in scenes ? How is it compared to signle objects?

make ICA separately for each block!!! or not at all...

Binding condition: triangle bleue vs other triangles, generalized to triangle bleue vs other triangles when the other object is blue (and optionally also not a triangle)



Preprocessing notes:
bad subjects (chance-level AUC) : 03 - 09 - 10 - 12 - 14
weird perf before t0: 07 - 11 - 13 - 16

to look into: 
sub01 - very weird looking PSD on 1, 2, 4 and 5, - run4: shity spiked dominating the evoked.
sub02 - run3 (one_obj). Weird very high amplitude ramping.
sub03 - run8 (1obj) : that asshole kept the button pressed for a whole trial. Maybe trials are shifted, resulting in bad decoding perf.
sub06: run1 one response was too quick. Wwird peak in the PSD at ! 12hz and ~30hz. Linked to construction work ? Run5-7-8-9: sustained diverging channels.
sub07 - run5 2obj: delay in one trial, maybe trials are shifted. Also same peak in PSD.
sub08 - run3 2obj: answered once during the presentation of the sentence. same peak in PSD.
sub09: same peak in PSD but even bigger. Huge ammount of noise in frontal sensors. Also looks like there is a jitter on the image presentation, also for sub08 but not really before (maybe on the onset but not the end).
sub10 - same jitter, same peak in PSD, also hige ammount of noise all over. 
sub11 - run1: one press during trial + run4 on during delay. run5: huge jumps + one huge fuckup in the triggers. Trials may be shifted. One button press at the begining of a trial in run6. Super fucking weird channel jumps in run7. On press before image on run 8. Super weird high sustained channels on run 9 + one press during the image. What a clusterfuck!
sub12 - run4 and 7: fckin weird ch jump. run5-6-7: weird peak in the PSD at 55hz... no run 10 ?
sub13 - run1: multiple answers during trials. Weird ch jumps in run7. No run10.
sub14: just fucking noisy all over.
sub15 - run1: button press jsut before a trial. run6: one trial is shifted. 
sub16: bit noisy but looks ok. problem in run8?



Meeting 27-10-20:
Mesure résumée : pool par fenetre après chaque mot et moyenne
La généralisation est-elle "diagonale" après le mot, ou bien off-diagonale ? Si on a un processing très différent on peut s'attendre a du off-diagonale. 
Decoder la position des mots ? Ou bien l'inclure dans la RSA. 
Inclure un flash sur tout l'écran, un truc gris ou salt and pepper, sur la moitié des essais, 500ms avant l'image. -> flash = trigger 5 
Figure finale : on veut un truc a la JR ou a la Pedro avec un timecourse par propriété d'interet: forme1, couleur1, relation, forme2, couleur2, objet individuels, match/nonmatch 
Trace de la prédiction pour chaque décodeur : quand est-ce que la perf est bonne ET quand est-ce qu'on a une réactivation. On peut les moyenner comme on le fait pour l'AUC. 
Quand on a une réactivation, est ce qu'elle est jointe pour nom et adj (forme et couleur), ou bien séparée ? Quel délai ? Comment sont ségrégées les réactivations d'un objet et de l'autre ? => binding problem.




Reprise en 2024 ! 
v1: replication (more or less, maybe check v34 for a closer replication)
v2: trying localizer crossmodal generalisation
v3: saving confusion matrices for RSA (9words, shapes and colors?) and full predictions for replays stuuuff.
Saving preds works. we save only the diagonal (within-time) predictions, but for the generalization across conditions of a different length (eg, loc to obj), then we have to save the whole matrix, which is fckin big. Maybe soon look into single clf gen? 

v4: trying single time point training and gen to specific window. 
v5: same with no timegen (no need for replays)

DONE 
find why the plotting script does not find all the timepoints

Img2WordCat = decoding images of colors vs images of shapes: super high performance! Gen is below chance.
Word2ImgCat Perf is similar to training on intra class decoding (eg: red vs other colors), and the gen is very strongly below chance ... what is happening? 


v6: problems solved, LRCV, just no null trials. Saving patterns and preds correctly 
v7: correct naming for outputs and also changing to l1 regularization -> many patterns are actally all zeroes. Why does this gets selected as the best regularization ??
v8: fixed regularization (l1) C = 0.1. Still using liblinear --- fucked up for some reason. Loooots of values are zeros. 
v9: solver=saga, l1C=.1 --- good? 
v10: solver=saga, l1C=crossvalidated. --- pretty much the same as v9 (with no crossval). 
v11: saga, C=.1, adding 50% of null trials. I added it is a somewhat weird way but that should be equivalent to what they did. They are set as a new class in the OVR, which is then discarded. Thus, all null trials are used as negative class for the training of each real class. 
Warning, does not work with equalize_events (would need some more work). It's fine because the magicians do not equalize trials (not even balance class weights actually ... but we do). 
-> good, kind of null correlation between subjects for scenes, very high positive correlation for localizer. 
v12: now null_prop=0.25. 

v13: now saving each trial's information, needed for reactivation analysis. null_prop=.5
v14: same with null_prop=.25
v15: C=1/0.006
Replay strength seems random. 

TODOs:
are the predicted probabilites predictive of the subject performances?
PAC over the course of the sentence? Or at least for 2 vs 5 words.

v16: still C=1/0.006, but not filtering by performance (we want also incorrect trials for the correlation with behavior), and doing multiple timepoints (.2, .3, .4, .6, .8).
no null trials. saving the original training time (from word onset) in the Label. That works!


Working version if the multiblock decoding! Training on all possible trials. With or without Relation
v17: adding the relation. Also increasing LogReg nb iteration from 10k to 1M. We have 10 x more trials for shapes and colors than relations, but shouldn't matter with classweights = balanced. 
Weights patterns look fine. Now this version is labelled PropertyAll, whereas the shapes and colors is just Property.

Hyperparameter optimization with 1vs6 training on localizer words and images and 1obj, and tested on 2obj, all positions. 
best set of params: [0.1, 'l1', 'liblinear', 'balanced'] (average perf over subject is maximal)
or [0.1, 'l1', 'saga', 'balanced'] (the numbef of subjects for which the perf is best is maximized).
Will use [0.1, 'l1', 'liblinear', 'balanced']. Makese sense to maximize the overall perf.
Also: sub-23 is at chance. REJECTING IT.
also remove: 
# 23: 0.5033565790831263
# 27: 0.5280319012834223
# 25: 0.5343305785730027
# 17: 0.5399329372520107
# 11_rb210035: 0.5400056092571954? 

v18: using the optimal parameters: [0.1, 'l1', 'liblinear', 'balanced'].
We now miss the most basic effect of more reactivations for present vs absent ...

v19: saga, C=100 - bof-ish, mais espoir avec 0.8? - ok ave preds et bon lien avec la performance ! 
v20: saga, C=100, classseights=None
v21: saga, C=1000, classseights='balanced' - also removing the test in one obj. - fuck launched as v20. probably overwrote the previous .
v22: removing training on one obj. - saga-c=100 --- better without one_obj! 
v23: not cat no smooth

Doing v500 = 500hz. Not very different from the 100hz versions. 


It seems that S1 and S2 are reactivated together AT THE SAME TIME. Check for the absent, maybe you'll have to average of something, to make sure that the order is always the same depending on whether there are 1 or 2 absent shapes. True for each training cond (just two_obj, up to loc-1obj-2obj).

Train position decoders?

Need to check all functions to make sure that the fact that we use only complex 2 trials is fine. Also find a way to use back all the trials.
Reactivations are between shapes and colors, never, it seems, across words and colors. 

v24: train a separate OVR for each property, because training all props together encourages the classifier to identifiy the category but not the items (each shape decoder is strongly correlated), leading to correlations in the predictions and false reactivation results. New argument: --split_props
v25: same with null trials, as much as 2obj trials (before the fix for getting as much null as needed).
Deleted 24 by accident.but have all the plots
Hope for t=.4, v25, training on all the data. 
But still some weird thing with the absent properties. Maybe just compare S1-C1 and S2-C2 vs S1-C2 and S2-C1

Use linear or gaussian SVM?
LR is good because it gives calibrated probabilities, that can be interpreted as the confidence in the decision (but maybe with you reactiation detection tecchnique that doesn't matter so much). 
SVM are in general better for generalization. Is this what we want? We generalize to a different setup, but precisely, it doesn't look like the training examples, sooo... 
Try SVM-G, if it works better then use it and cite: "machine learning" by Andrew NG. It can be found in week 7 at the end of: "Support vector machines - using an SVM"
sources: https://stats.stackexchange.com/questions/95340/comparing-svm-and-logistic-regression#95348 

added back the 4 "bad" subjects and launched the hyperparm optim for SVC-rbf: (overall the perf on this is slightly better than with LR).
clf = SVC(kernel='rbf', class_weight='balanced', max_iter=-1, C=1, gamma=0.001, probability=True, random_state=42)

v26: with the best decoder from the grisearch = [0.1, 'l1', 'saga', 'balanced']. Also null_prop=0.1 (after the fix), so that it's most similar to v25.
->: stop training on 2obj only, and focus on 0.4s. Also, synchronous does not seem different for the present props
Kind of similar to v25 for seq, but worse for sync.
v27: same as v26 but with the old way to make null trials (single null trial per trial). (or it could be that you added back the shitty subjects)
v28: SVC-rbf - mouais, pas mieux.
v29: back to LR, with best params. Removing bad subjects. single null trial per trial. Mouais
v30: adding more null trials null_prop=0.5 (true 50% of tests). Not too bad, weights are ok-ish.
v31: same with null_prop=0.25
v32: same with no baseline. not better, and weights correlations are weird.
v33: back to baselining, null_prop=0.1
v34: again with no null_prop
v35: same with equalize events to avoid the slight bias in weights correlations (triangle vert, cerlce bleu, carre rouge). Still there although weaker, but the sequential react is worse.

try the hyperparam optim for t=0.4s?
try looking for SL-SR (left-right) instead of first-second! If there is a specific order with the relation, it might only pop up then. 


Change the structure of the code? Have 2 lists, present and absent, of various length each, to take into account trials of all complexities. Then you need to find a way to keep only the trial with no bias for the coactivations. If there is a repeated word, then it should not count as coactivation with itself.
-> works with a solve that gets the label of each props for each trial, not just the state nb. 
-> seems good for the first NP, but near nothing for the second.
Shapes never co-activate with other shapes. S1 can coactivate with C2, but not S2...
Keeping the 2s is never good.

v36: null_prop=0.1, LR = [100, 'l1', 'saga', 'balanced']
v37: same with 500hz

HUUUUGE problem with the way significant reactivation is computed. With the zscore or percentile per trial, we get THE EXACT SAME NUMBER OF REACTIVATION FOR EACH TRIAL. Trying with raw probs. Fixed threshold for all subject (or per subject?)

Going back to Prop (not PropAll) because it makes the selection of significant probabilities for all trials (maybe all subjects) easier. 
in v38
Not great. Not off-diagonal coactivations. maybe one issue is that when shape is repeated, it is assigned to shape1, so there are much more of those.

v39: filter with Perf=1: not better
v40: cat5 (nofilter): slightly better
v41: no cat, no filter, no_null, all subjects, multi times. PropAll but forgot to change the query. 
v42: proper PropAll no cat, no filter, no_null, all subjects, multi times.

Now, with the threshold, the reactivations are dominated by Relation. Make threshold separate for each prop? 
Should the threshold be in common for shapes, or separate for each?

Why zeros in subdiagonals in synchrnous ? We force diag to be zeros, but that should be it.

Trying to normalize each property before running the whole analysis pipeline.

add back the null trials because its weird to have no coactivations for the same prop. Ever. 

v43: allsubs, null_prop=0.1, PropAll and WordPos, filter=1Perf. Woops. Overwrote 42.

Run test on one_obj as well? 
make the function polyvalent, work for one word as well? 

Removing '23' '27' '11_rb210035' that have super weird activations (LR failed to converge probably)

v44: mean5 to get smoother reactivations. Removed bad subs. Filter Perf=1.
Smooth is good. Added null_prop =0.25 to v44.

v45: null_ptop = 0 for props, 0.5 for wordPos. Filter. 
Overlap duration seems bigger for S1C1 and for S2C2.

Correlation between position and property? (for cmplex2 trials)


Reactivations are always dominated by Relation, so better not show it. 

v46: null_prop=0.25 for wordPos, back to best classifier from optim. still filter. 
added the correlation, but works only for complexity=2 trials because of the shape thing.

v47: null_prop=0.25 for props (to get orthogonal reactivations) and 0.1 for wordPos to get less correlation between adjacent words. 
-> to high a correlation between props? Shapes are very anticorelated. So,add MORE null trials??


v48: null_prop=0.5 for props
done on v47... redoing.
Whatever I do, the corelation between shapes is -0.5. I think its by design.

best = complex2 - min4length - 10lag

v49: same with C=100
Try without filter to correlate with performance ! 


chercher forme et couleur ensemble, pour NP1 et NP2
prop(triangle rouge) > prop(paires pas presentes)
Pour les compexite 2 : triangle bleu a gauche de carre rouge
une paire pas presente du tout : cercle vert
2 paires presente mais pas ensemble : triangle rouge et carre bleu
4 paires a moitie presente: triangle vert et carre vert; cercle bleu, cercle rouge


comportement: regarder les probas specifiques a la relation pour les essais qui s'attaque a la relation. (mismatch 2)
relatifs a la propriété qui est changée sur les property mismatch. Sur les NP pour les relations mismatch. 
Maybe filter, keep only subjects that have below 80% perf. 

Is the relation reactivated along with their corresponding object? Even for the relation that was not presented in this trial? 
eg: Triangle bleu a G de carré rouge. Triangle bleu et G devrait etre coactivés, mais aussi carré rouge et D.
Relation: left relation followed by the right object? 

less property reactivation -> more errors on mismatch 1 
less co-activation for bound items -> more errors on binding mismatches
less coactivation of objects with their corresponding relation -> more relation mistakes. 

Look at trials where an event was found! timelock onit?
PAC for different complexity! 


v44 trained on obej with timegen. No better performance when the color is presented than when the shape is. Is it a true object decoder? 

v45: train many stuff on Loc. 
Loc_cat to get a shape or color decoder. What for?
Loc_cross to get a general decoder. Compare to Loc_word et Loc_img. For both shapes, colors, and both together. 
train on words vs images to see whether the reactivations are more word-like or image-like! But there might be a confound due to the alternation of words and imags..
Peak is often earlier for images than words. often there are 2 peaks, .2s and .4s for images. partial generalization between the two. 
get the peak for each instead of hardcoding 400ms ? 

More reactivation for word or image decoders? Or train with both? 

All is better and cleaner than shape or color separately for images and all, but not for words. Train on images is better. Train on both is good, especially with All (both shapes and colors). Category is similar but a little bit better.
But needs to split for each categroy to check that the performance is not biased towards images or shapes for ex.